import Layout from "../../../components/layout/layout";
import SignupMode from "../../../components/loginform/signupMode";

const Signup = () => {
  return (
    <Layout>
      <SignupMode />
    </Layout>
  );
};

export default Signup;
