import Layout from "../../../components/layout/layout";
import LoginMode from "../../../components/loginform/loginMode";

const Login = () => {
  
  return (
    <Layout>
      <LoginMode />
    </Layout>
  );
};

export default Login;
